# commit

```bash
$ git commit -m "..."
# автоматически под­готавливать все неподготовленные, прослеженные изменения файла, в том числе удаление отслеживаемых файлов из рабочей копии, перед осущест­влением фиксации
$ git add -a -m "..."

```
